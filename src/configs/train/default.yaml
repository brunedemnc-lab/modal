# @package _global_
training:
  batch_size: 16
  lr: 0.0001
  epochs: 15
  num_workers: 16
  # Where to save the best checkpoint (by validation accuracy)
  checkpoint_path: ${hydra:runtime.cwd}/20_epochs/best_model.pt
  device: cuda
